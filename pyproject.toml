[project]
name = "cancer-segmentation"
version = "0.1.0"
description = "Segmentation models for cancer cells"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.10.0",
]

[dependency-groups]
dev = [
    "fire>=0.7.1",
    "pytest>=9.0.2",
]
